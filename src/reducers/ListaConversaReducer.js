const INITIAL_STATE = {};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// Aula 277
// Trocando Mensagens - Parte 1 - Criando ListaConversaReducer
